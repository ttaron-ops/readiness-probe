# Go
bin/
*.exe
*.test
*.out
vendor/

# Python (cost scripts, notebooks)
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Secrets & local env — never commit cloud/GPU credentials
.env
*.local
*.pem
*.key
kubeconfig
*.kubeconfig

# Large / generated artifacts
*.log
tmp/
scratch/

# Upstream repos cloned locally for source verification while writing
# lessons (this environment's egress proxy blocks several doc domains, so
# lessons are verified against source instead). Not part of the course.
/deepspeed/
/torchtitan/
/ironic/
/nhc/
